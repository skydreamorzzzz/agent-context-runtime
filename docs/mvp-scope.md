# MVP scope

The supported experiment is a synchronous, from-scratch paired run with one provider, one taskset adapter, raw evidence capture, noop, and one deterministic intervention: `omit_one_duplicate_read_v1`.

Out of scope: checkpoint restore, concurrent execution, semantic compression, learned policies, multiple interventions, distributed orchestration, and dashboard/database infrastructure.
